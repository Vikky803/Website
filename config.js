/**
 * Heaven Manager dashboard — frontend config.
 *
 * This is the ONE file you need to edit to connect the dashboard to
 * your bot. Everything else works as-is.
 */
window.HEAVEN_CONFIG = {
  // Where your dashboard server (the /server folder) is running.
  // Local testing:      'http://localhost:3001'
  // Deployed elsewhere:  'https://your-dashboard-server.example.com'
  API_BASE: 'http://localhost:3001',
};
